cài sass:
    npm install --save-exact sass@1.48.0

tạo file src/App.scss

copy nội dung file src/App.css vào file src/App.scss

xóa file App.css

stop project và chạy lại project (npm start) để project sử dụng được scss

tạo thư mục src/components

tạo thư mục src/components/Login

tạo thư mục src/components/Navigation

tạo file Nav.js trong thư mục src/components/Navigation

tạo file Nav.scss trong thư mục src/components/Navigation

xóa file index.css

xóa dòng import './index.css'; trong file index.js

npm install --save-exact react-router-dom@5.3.0

npm install --save-exact bootstrap@5.1.3

import bootstrap vào file index.js để sử dụng bootstrap cả project:
    import 'bootstrap/dist/css/bootstrap.min.css'

Form register: để chuyển trang có thể sử dụng thẻ Link của react-router-dom:
    <Link to='/register'>
        <button className='btn btn-success'>
            Create new account
        </button>
    </Link>
hoặc sử dụng hook useHistory() của react-router-dom:
    import { useHistory } from 'react-router-dom'
    let history = useHistory()
    const handleCreateNewAccount = () => {
        history.push('/register')
    }
    <button className='btn btn-success'
        onClick={() => handleCreateNewAccount()}>
        Create new account
    </button>

npm install --save-exact axios@0.25.0

sử dụng website https://reqres.in/ để giả lập API
    import { useEffect } from 'react'
    useEffect(() => {
        axios.get("https://reqres.in/api/users?page=2").then(data => {
            console.log('>>> check data axios: ', data)
        })
    }, [])

Tạo API trên 01_jwtBackend để test axios từ 02_jwtFrontend:
    mở file web.js trong project 01_jwtBackend thêm vào dòng sau:
        import apiController from "../controllers/apiController";
        router.get("/api/test-api",apiController.testApi)
    tạo file src/controller/apiController.js trong project 01_jwtBackend với nội dung sau:
        const testApi = (req, res) => {
                return res.status(200).json({
                    message: 'ok',
                    data: 'test api'
                })
            }
        module.exports = {
            testApi
        }
    mở trên trình duyệt địa chỉ http://localhost:8080/api/test-api, nếu thành công thì sẽ nhận được dữ liệu như sau:
        {"message":"ok","data":"test api"}

Mở file Register.js trong project 02_jwtFrontend:
    sửa: https://reqres.in/api/users?page=2
    thành: http://localhost:8080/api/test-api

mở trên trình duyệt địa chỉ http://localhost:3000/register thì sẽ bị lỗi CORS

cách fix lỗi CORS cho project 01_jwtBackend:
    mở file .env trong project 01_jwtBackend và thêm vào dòng sau:
        REACT_URL='http://localhost:3000'
    mở file server.js trong project 01_jwtBackend và thêm vào đoạn code dưới đây sau dòng const app = express():
        app.use(function (req, res, next) {

            // Website you wish to allow to connect
            res.setHeader('Access-Control-Allow-Origin', process.env.REACT_URL);

            // Request methods you wish to allow
            res.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS, PUT, PATCH, DELETE');

            // Request headers you wish to allow
            res.setHeader('Access-Control-Allow-Headers', 'X-Requested-With,content-type');

            // Set to true if you need the website to include cookies in the requests sent
            // to the API (e.g. in case you use sessions)
            res.setHeader('Access-Control-Allow-Credentials', true);

            // Pass to next layer of middleware
            next();
        });
    stop và chạy lại project 01_jwtBackend
    mở trên trình duyệt địa chỉ http://localhost:3000/register để xem kết quả đã fix được lỗi CORS chưa (xem trong console sẽ mất lỗi CORS)

npm install --save-exact react-toastify@8.1.0

npm install --save-exact lodash@4.17.21

npm install --save-exact react-paginate@8.1.0

npm install --save-exact react-bootstrap@2.1.1

npm install --save-exact font-awesome@4.7.0

mở file index.js thêm vào dòng code sau để sử dụng font-awesome:
    import 'font-awesome/css/font-awesome.min.css'

tạo file src/setup/axios.js để cấu hình lại axios theo ý muốn
